!-- Section: Partner Details Grid -->
<div class="section-title">WHAT I LOVE ABOUT MY PARTNER</div>
<div class="partner-grid">
    
    <!-- Box 1: Personality -->
    <div class="partner-box" onclick="toggleBox(this)">
        <h3>Personality</h3>
        <div class="partner-content">
           He is really transparent and honest with me. He is kind, funny, caring, reliable, good listener, great friend, and a great boyfriend.<br>
           -When i said he is transparent, I mean it when everytime he is busy with something and he there are times where he needs his time as well he would tell me openly about it.
           -He is honest, There are times where i would ask him about his thoughts about something and he would be open about it and say that he doesn't like it, I like it when he does tell me so i would know.
           -He would be there for you to accompany you when you play a game, a good friend you could play with.
           -He is a great boyfriend so far, spending so much time with me and even cheering me up when i feel a little down. THank you again.
           We do make mistakes and hopefully you won't dwell on it as much cause i am also learning not to with my mistakes.
        </div>
    </div>

    <!-- Box 2: Appearance -->
    <div class="partner-box" onclick="toggleBox(this)">
        <h3>Appearance</h3>
        <div class="partner-content">
            First of all, he is really handsome. He has a great facial features like a shaped nose, charming dark brown eyes, really fluffy hair( I wish i could fluff it ), Thick eyebrows which fits overall structure,
            a really cool beared and mustache and has pink kissable lips.(AAAAhhhhhh)
            He is on the taller side and has chubby features that i really love. He also has THICK ass, may i slap em? LOL
            And... He has a really great smile which shows his adorable and cute side.
        </div>
    </div>

    <!-- Box 3: Skills -->
    <div class="partner-box" onclick="toggleBox(this)">
        <h3>Skills & Traits</h3>
        <div class="partner-content">
            -Can cook
            -Plays games
            -Is really good at playing games too
            -Has great music taste
            -No problem with english
            -Draws really cutely\
            -Can sing great
            -Is really funny potential to be a great comedian someday XD
            -Has also good vocal range and should start a channel
            -Has the charms which other would either be attracted to or finds it funny.
        </div>
    </div>

    <!-- Box 4: My Heart -->
    <div class="partner-box" onclick="toggleBox(this)">
        <h3>Memories</h3>
        <div class="partner-content">
        I will mention all of my favorite memories of us together here:
        <br> -Our first ever interaction in roblox, which was in "Please Donate!" I was strolling around and looking at people who needed donations when i stumbled upon your cool looking
        avatar and when i read your sign I donated you to meow for robux(which was literally what you said), this was and will be my most favorite because that same day... 
        you were donated 6,000+ robuck just to reach your goal then you meowed 6k times literally rapid fire it. Honestly I am really glad i was there at that time, I don't regret sleeping late at that time.
        <br>-That one minecraft session together late at night just talking about personal stories and comforting each other as well. This was also the same day where you found out I listened to weird shiz. And the same day where you told me about your experiences.
        <br>-Another minecraft session where we were talking about so many things and the hug as well, thank you for that!
        <br>-A fun date but tiring for you cause you had to walk a lot, it will be one of the great memories we can look back to in the future. I stayed up so late and made popcorn just so i could watch you show me around the city
        views it was really amazing being there to accompany you on your walk. It was also the same day you bought really comfy shoes!
    </div>
    </div>
    <!-- Otto -->
     <div class="partner-box" onclick="toggleBox(this)">
        <h3>His really cute dog named Otto</h3>
        <div class="partner-content">
            <img src="/pictures/otter.png" alt="Otto the German Doggy" style="width: 100%; max-height: 200px; object-fit: cover; border-radius: 10px; margin-top: 10px;">
        </div>
    </div>

</div>

////html code for index.html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Our Digital Sanctuary</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="container">
        <!-- Your Side -->
        <div class="side left">
            <h1>My World</h1>
            <p>"Life is better when we're playing together."</p>
            <a href="nin-page/my-profile.html" class="btn">My Profile</a>
        </div>

        <!-- The Bridge -->
        <div class="bridge">
            <div class="timer" id="days-together" aria-live="polite">Loading...</div>
            <p class="subtext">Together</p>
            <a href="home.html" class="btn enter-btn">ENTER</a>
        </div>

        <!-- His Side -->
        <div class="side right">
            <h1>His World</h1>
            <p>"My favorite player two for every game."</p>
            <a href="emma-page/my-profile.html" class="btn">My Profile</a>
        </div>
    </div>

    <script>
        const relationshipStartedAt = new Date('2026-06-09T12:32:00-04:00');
        const millisecondsPerDay = 24 * 60 * 60 * 1000;
        const counter = document.getElementById('days-together');

        function updateDaysTogether() {
            const elapsedTime = Date.now() - relationshipStartedAt.getTime();
            const daysTogether = Math.max(0, Math.floor(elapsedTime / millisecondsPerDay));
            counter.textContent = `${daysTogether} ${daysTogether === 1 ? 'Day' : 'Days'}`;
        }

        updateDaysTogether();
        setInterval(updateDaysTogether, 60 * 1000);
    </script>
</body>
</html>



import { db, messaging, VAPID_KEY } from "/firebase-init.js";
import {
  doc, getDoc, setDoc, updateDoc, onSnapshot,
  collection, query, where, increment
} from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";
import { getToken } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-messaging.js";

// Which person is on this page. index.html leaves window.HUG_ROLE unset,
// so it defaults to partner_a. His page sets window.HUG_ROLE = "partner_b"
// in an inline <script> tag right before this file loads.
const MY_ID = window.HUG_ROLE === "partner_b" ? "partner_b" : "partner_a";
const PARTNER_ID = MY_ID === "partner_a" ? "partner_b" : "partner_a";

const statusRef = doc(db, "hugs", "status");
const counterRef = doc(db, "hugs", "counter");
const tokenRef = doc(db, "tokens", MY_ID);

// Uses UTC instead of each device's local clock
function todayStr() {
  const d = new Date();
  return `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, "0")}-${String(d.getUTCDate()).padStart(2, "0")}`;
}

function yesterdayStr() {
  const d = new Date();
  d.setUTCDate(d.getUTCDate() - 1);
  return `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, "0")}-${String(d.getUTCDate()).padStart(2, "0")}`;
}

// ---------- Register this device for push notifications ----------
async function registerForPush() {
  try {
    const permission = await Notification.requestPermission();
    if (permission !== "granted") return;

    const registration = await navigator.serviceWorker.register("/firebase-messaging-sw.js");
    const token = await getToken(messaging, {
      vapidKey: VAPID_KEY,
      serviceWorkerRegistration: registration
    });

    if (token) {
      await setDoc(tokenRef, { token, updatedAt: Date.now() });
    }
  } catch (err) {
    console.warn("Push registration skipped:", err);
  }
}

// ---------- Streak & Counter display ----------
function renderStreak(streak) {
  const el = document.getElementById("hug-streak");
  if (!el) return;
  if (!streak || streak < 1) {
    el.textContent = "No streak yet — send the first hug!";
  } else {
    el.textContent = `🔥 ${streak} day streak`;
  }
}

function updateButtonState(lastHugDate) {
  const btn = document.getElementById("hug-btn");
  if (!btn) return;
  const already = lastHugDate === todayStr();
  btn.disabled = already;
  btn.style.opacity = already ? "0.6" : "1";
  btn.style.cursor = already ? "not-allowed" : "pointer";

  const label = document.getElementById("hug-btn-label");
  if (label) {
    label.textContent = already ? "Come back tomorrow 💤" : "Send a Virtual Hug";
  }
}

// Live-update UI whenever the shared status doc changes
onSnapshot(statusRef, (snap) => {
  const data = snap.data() || {};
  renderStreak(data.streak || 0);
  updateButtonState(data.lastHugDate || null);
});

// Live-update global counter badge
onSnapshot(counterRef, (docSnap) => {
  const hugCountSpan = document.getElementById("hug-count");
  if (docSnap.exists() && hugCountSpan) {
    hugCountSpan.textContent = docSnap.data().total || 0;
  }
});

// ---------- "You got a hug" box ----------
function formatReceivedTime(ms) {
  const diffMinutes = Math.round((Date.now() - ms) / 60000);
  if (diffMinutes < 1) return "just now";
  if (diffMinutes < 60) return `${diffMinutes} min ago`;
  const diffHours = Math.round(diffMinutes / 60);
  if (diffHours < 24) return `${diffHours} hr ago`;
  const d = new Date(ms);
  return d.toLocaleDateString(undefined, { month: "short", day: "numeric" });
}

function renderReceivedBox(latestEvent) {
  const box = document.getElementById("hug-received-box");
  if (!box) return;

  if (!latestEvent) {
    box.textContent = "No hugs received yet";
    box.classList.remove("has-hug");
    return;
  }

  box.textContent = `🤗 You got a hug ${formatReceivedTime(latestEvent.sentAt)}`;
  box.classList.add("has-hug");
}

// Listens only for hugs sent TO this specific person
const receivedQuery = query(collection(db, "hugEvents"), where("to", "==", MY_ID));
onSnapshot(receivedQuery, (snap) => {
  let latest = null;
  snap.forEach((docSnap) => {
    const data = docSnap.data();
    if (!latest || data.sentAt > latest.sentAt) latest = data;
  });
  renderReceivedBox(latest);
});

// ---------- Sending a hug (limit + streak + routing) ----------
async function sendHug() {
  const snap = await getDoc(statusRef);
  const data = snap.exists() ? snap.data() : {};
  const today = todayStr();

  if (data.lastHugDate === today) {
    return false; // already used today's hug
  }

  let newStreak = 1;
  if (data.lastHugDate === yesterdayStr()) {
    newStreak = (data.streak || 0) + 1;
  }

  // Update status and streak
  await setDoc(statusRef, {
    lastHugDate: today,
    streak: newStreak,
    lastSender: MY_ID
  }, { merge: true });

  // Increment total counter safely
  await setDoc(counterRef, { total: increment(1) }, { merge: true });

  // Send event targeted explicitly to your partner
  await setDoc(doc(db, "hugEvents", `${MY_ID}_${Date.now()}`), {
    from: MY_ID,
    to: PARTNER_ID,
    sentAt: Date.now()
  });

  return true;
}

// ---------- Floating animation logic ----------
function triggerHugAnimation() {
  let container = document.getElementById("hug-animation-container");
  if (!container) {
    container = document.createElement("div");
    container.id = "hug-animation-container";
    document.body.appendChild(container);
  }

  const hugGraphic = document.createElement("img");
  hugGraphic.src = "pictures/heart.png"; 
  hugGraphic.className = "floating-hug-img";
  
  container.appendChild(hugGraphic);

  setTimeout(() => {
    hugGraphic.remove();
  }, 1200);
}

// ---------- Wire up the button ----------
document.addEventListener("DOMContentLoaded", () => {
  registerForPush();

  const hugButton = document.getElementById("hug-btn");
  if (!hugButton) return;

  hugButton.addEventListener("click", async (e) => {
    const allowed = await sendHug();
    if (!allowed) {
      e.stopImmediatePropagation();
      return;
    }
    
    // Trigger your pop-up graphic animation
    triggerHugAnimation();
  });
});